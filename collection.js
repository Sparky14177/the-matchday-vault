(async function(){const grid=document.getElementById('grid'),noRes=document.getElementById('no-results');let items=[];try{items=await (await fetch('assets/data/items.json')).json();}catch(e){}function card(it){return `<a class="card" href="item.html?id=${encodeURIComponent(it.id)}"><div class="thumb"><img loading="lazy" src="${it.cover}" alt=""></div><div class="pad"><div class="eyebrow">${it.type||''}</div><h4 style="margin:.25rem 0 .35rem">${it.title}</h4><p class="muted">${[it.player,it.team,it.year].filter(Boolean).join(' · ')}</p></div></a>`;} if(!items.length){noRes.hidden=false;return;} grid.innerHTML=items.map(card).join('');})();